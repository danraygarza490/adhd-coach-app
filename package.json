{
  "name": "adhd-coach-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "seed": "npx tsx scripts/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^16.4.7",
    "drizzle-kit": "^0.30.2",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.3",
    "openai": "^4.104.0",
    "postgres": "^3.4.5"
  }
}
